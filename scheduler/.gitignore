# AWS SAM build artefacts
.aws-sam/
samconfig.toml.local

# Node
node_modules/
backend/node_modules/
*.log
npm-debug.log*

# Environment / secrets — never commit real credentials
.env
.env.*
!.env.example

# macOS
.DS_Store
